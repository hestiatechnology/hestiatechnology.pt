---
import "@/styles/global.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";
import { AnnouncementBar } from "@/components/annoucement-bar";

const { title } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="sitemap" href="/sitemap-index.xml" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />

    <title>{title}</title>
    <meta
      name="description"
      content="Aumente a eficiência e o controlo. O ERP da Hestia integra os seus processos essenciais para operações fluidas."
    />

    <meta property="og:title" content="Hestia Technology Lda" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://hestiatechnology.pt/" />
    <meta
      property="og:description"
      content="Welcome to Hestia Technology Lda"
    />

    <link
      href="https://calendar.google.com/calendar/scheduling-button-script.css"
      rel="stylesheet"
    />
    <script
      src="https://calendar.google.com/calendar/scheduling-button-script.js"
      async></script>
  </head>
  <body class="min-h-screen flex flex-col antialiased">
    <AnnouncementBar
      message="🎉 We are looking for investors!"
      type="promotion"
      actionText="Find More"
      actionHref="/investments"
    />
    <Header client:only="react" />
    <main class="flex-1">
      <slot />
    </main>
    <Footer client:only="react" />
  </body>
</html>

<script is:inline>
  const getThemePreference = () => {
    if (typeof localStorage !== "undefined" && localStorage.getItem("theme")) {
      return localStorage.getItem("theme");
    }
    return window.matchMedia("(prefers-color-scheme: dark)").matches
      ? "dark"
      : "light";
  };
  const isDark = getThemePreference() === "dark";
  document.documentElement.classList[isDark ? "add" : "remove"]("dark");

  if (typeof localStorage !== "undefined") {
    const observer = new MutationObserver(() => {
      const isDark = document.documentElement.classList.contains("dark");
      localStorage.setItem("theme", isDark ? "dark" : "light");
    });
    observer.observe(document.documentElement, {
      attributes: true,
      attributeFilter: ["class"],
    });
  }
</script>
