---
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent } from "@/components/ui/card";
import { Calendar, User } from "lucide-react";

export async function getStaticPaths() {
  const posts = await getCollection("blog", ({ id }) => id.startsWith("en/"));
  return posts.map((post) => ({
    params: { slug: post.slug.split("/")[1] },
    props: { post },
  }));
}

const { post } = Astro.props;
const { Content } = await post.render();
---

<Layout title={post.data.title}>
  <div class="min-h-screen bg-muted/30">
    <div class="container mx-auto px-4 md:px-6 py-12 md:py-20 max-w-4xl">
      <div class="mb-8 text-center">
        <div class="flex justify-center gap-2 mb-4">
            {post.data.tags?.map(tag => <Badge variant="secondary">{tag}</Badge>)}
        </div>
        <h1 class="text-4xl md:text-5xl font-bold tracking-tight text-foreground mb-6 leading-tight">
            {post.data.title}
        </h1>
        <div class="flex items-center justify-center gap-6 text-muted-foreground text-sm">
            <div class="flex items-center gap-2">
                <Calendar class="w-4 h-4" />
                {post.data.date.toLocaleDateString('en-US')}
            </div>
            <div class="flex items-center gap-2">
                <User class="w-4 h-4" />
                {post.data.author}
            </div>
        </div>
      </div>

      <Card class="border shadow-lg overflow-hidden">
        <CardContent class="p-8 md:p-12">
            <div class="prose prose-lg dark:prose-invert max-w-none prose-headings:text-foreground prose-p:text-muted-foreground prose-strong:text-foreground prose-a:text-primary hover:prose-a:text-primary/80 transition-colors">
                <Content />
            </div>
        </CardContent>
      </Card>
    </div>
  </div>
</Layout>
